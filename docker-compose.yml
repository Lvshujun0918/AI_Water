version: '3.8'

services:
  app:
    image: ghcr.io/lvshujun0918/ai_water:main
    ports:
      - "8080:80"
    volumes:
      - ./backend/uploads:/app/backend/uploads
    environment:
      - NODE_ENV=production