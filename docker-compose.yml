version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "8080:8080"
    volumes:
      - ./backend/uploads:/app/backend/uploads
      - ./backend/users.db:/app/backend/users.db
    environment:
      - NODE_ENV=production
