services:
  app:
    image: ghcr.nju.edu.cn/lvshujun0918/ai_water-riscv:latest-riscv
    user: "1000:1000"
    ports:
      - "8080:80"
    volumes:
      - ./uploads:/app/backend/uploads
      - ./db:/app/backend/db
    environment:
      - NODE_ENV=production